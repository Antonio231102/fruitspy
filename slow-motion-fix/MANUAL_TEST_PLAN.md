# Manual runtime validation

These steps require manual execution on an emulator or physical device. For diagnostic builds, request `--report PATH` and retain the generated JSON report because the endpoint and per-user signing identity make each installable APK environment-specific. Ordinary patching writes only the APK. Exact-duration tests are waived by user decision: qualitative normal pace plus emulator-to-real-device LAN synchronization is the speed acceptance gate.

As of 2026-09-09, the owner accepts the current slow-motion patch as fully functional with high confidence. The procedures below preserve the original qualification workflow and are available for issue reproduction, not a new pre-release testing obligation. Long-session investigation is deferred until user reports identify a problem. No further x86 emulator or actual ARMv5/ARMv6 device testing is planned; game UI actions remain user-only.

## Test artifacts

Baseline:

```text
User-supplied original Fruit Ninja 1.7.6 APK
SHA-256 5e94d16234504f5d2b6948b59371d8535c4364249b76e2533bba09114c808650
```

Candidate:

```text
APK generated by python patcher/patch_apk.py --report PATH
SHA-256 recorded in the diagnostic JSON report explicitly selected by PATH
```

Both APKs use package `com.halfbrick.fruitninja`, but the original and locally generated APK normally have different signing certificates. Back up saves before changing installations. APKs generated later with the same persistent local FruitSpy key can update one another.

## Record the environment

Before each run, record:

- Device model and SoC
- Android version/API level
- Display refresh-rate setting
- Battery/performance mode
- Whether the device is charging
- Whether automatic date and time is enabled
- APK SHA-256 under test

Keep the display refresh rate and power settings unchanged between baseline and candidate measurements.

## Baseline observation

1. Install the baseline APK if it is not already installed.
2. Fully stop and reopen the app.
3. Observe whether fruit movement, menu animation, particles, and sound appear uniformly slowed or merely stuttery.
4. Exact countdown-duration measurements are not required.

A simulation-clock defect should make the in-game countdown and motion advance more slowly than real time. GPU jank alone should drop frames without proportionally extending the game timer.

## Install the candidate

An update installation works when the installed APK and candidate use the same persistent local FruitSpy signing key:

```powershell
adb install -r "PATH_TO_PATCHED_APK"
```

Android 14 or newer may require:

```powershell
adb install --bypass-low-target-sdk-block -r "PATH_TO_PATCHED_APK"
```

If the device has the original application or a build signed by another key, preserve its data before uninstalling it. Android rejects an update whose signing identity differs.

## Primary acceptance test

1. Fully stop and reopen the patched app.
2. Observe game pace under the same device settings as the baseline.
3. Verify emulator-to-real-device LAN synchronization using the online test below.
4. Observe fruit trajectories, countdown speed, particles, UI animation, and audio synchronization.

Primary pass criteria:

- A full LAN match between the final Unified x86 emulator build and a real device stays synchronized.
- Fruit and UI motion no longer appear uniformly slow.
- Audio remains synchronized.
- No new in-scope crash, freeze, or severe stutter occurs.

The user has confirmed normal x86 pace with very strong confidence. Background/resume and sleep/wake also passed by user report. The final Unified APK subsequently completed three consecutive games between the Android 9 Pixel_3a x86 AVD and Galaxy S4 on Android 4.4.2, with no desynchronization or disconnects reported. This satisfies the final emulator-to-real-device synchronization gate. Exact round and interruption durations were not recorded; do not infer measured timing, both hosting directions, or direct versus relayed transport from these reports.

Known emulator color corruption and crashes are excluded from project scope under the user's Intel-driver assumption. Color corruption also reproduced with the untouched APK, and Genymotion did not resolve the issues. This is not proof of the precise cause of every crash or of crash-free operation.

## Lifecycle safety tests

The hardened candidate uses a monotonic clock and replaces frame gaps above 250 ms with one normal 16.667 ms frame. Verify the guard behavior:

1. Start a round, background the app for 30 seconds, and resume it.
2. Repeat by locking the screen for 30 seconds and unlocking it.
3. Rotate or trigger another activity only if normal device/game use permits it.
4. Confirm the next frame does not jump physics forward, instantly end the round, eject all fruit, corrupt audio, freeze, or crash.
5. Return to the menu and start another round.

Any large one-frame jump indicates that the injected guard did not execute as designed. A freeze or crash may indicate an ABI-specific syscall or injected-code defect.

## Display and load tests

Where the device supports them:

1. Repeat once at 60 Hz.
2. Repeat once at the highest available refresh rate.
3. Repeat with normal battery mode; avoid comparing a power-saving baseline against a performance-mode candidate.
4. Play a visually busy mode and distinguish uniform slow motion from intermittent dropped frames.

Correct elapsed-time simulation should preserve game speed even when rendering misses frames.

## Online test

After qualitative offline speed confirmation:

1. Use the final Unified x86 build on the emulator and a real device as the peer, with the candidate installed on both when practical.
2. Test each player as host.
3. Complete at least one full LAN round between the emulator and real device.
4. Confirm both clients' countdowns and fruit motion remain synchronized.
5. Leave the room connected beyond six minutes, then continue manually.

A mixed patched/unpatched match is useful diagnostically but is not the preferred final configuration because the two clients may advance simulations at different rates.

## Long-session investigation — deferred until user reports

The current patch is accepted without a 72-minute physical endurance test. Accelerated execution of the actual timer paths already verified wrap and guard boundaries across all three ABIs.

If a user reports a long-session issue, preserve the build and device details below, reproduce the reported conditions, and inspect countdown, motion and audio around the failure. The low-word clock follows system monotonic time, not time since app launch; a 72-minute wait is not automatically the appropriate reproduction.

System wall-clock changes remain unnecessary for the hardened implementation because `CLOCK_MONOTONIC` is independent of civil time.

## Failure evidence

If a test fails, preserve:

- Exact APK SHA-256
- Device/API/refresh-rate details
- Real duration versus in-game duration
- Whether failure followed launch, background/resume, screen lock, or online connection
- A screen recording when possible
- Relevant `adb logcat` output from the run

Do not modify the APK between a failed run and collecting its hash.
